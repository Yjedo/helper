server:
  port: 8081
spring:
  application:
    name: helper-gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yaml